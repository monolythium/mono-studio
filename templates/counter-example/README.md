# Counter Example

Minimal MRV contract template with an increment call and a read call.

```bash
mono-dev new Counter --template counter-example --out /tmp
mono-dev build /tmp/counter
mono-dev simulate /tmp/counter
```
